# Build outputs
/lib/
/bin/
/builds/
/logs/
*.o
*.a
*.obj
*.lib
*.exe
*.pdb
shard.lock

# Full upstream clones kept on disk for reference only:
# egui (architecture reference — docs/ANALYSIS.md), sokol, fontstash.
# The headers the build actually needs are vendored in vendor/.
.ref/

egui-upstream/
